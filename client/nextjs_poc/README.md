# Next.js Web Client POC

This is a minimal proof-of-concept Next.js web client for ATP framework integration.

## Usage

To run:
1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`

## Test

See `../../tests/test_nextjs_poc.py` for a basic startup test.
