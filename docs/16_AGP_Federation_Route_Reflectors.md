# 16 — AGP Federation & Route Reflectors

- iAGP/eAGP peering, communities, damping, and freshness timestamps.
- Route Reflector role to avoid full mesh; security with signed attestations.
- Budget RLH per hop and per-domain policy constraints.
- What-if tooling for policy changes and convergence checks.
